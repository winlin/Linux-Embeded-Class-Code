	该程序最主要的点就是：点阵数据的获取 和 存储到一维数组的对应位置的转化
	我们采用的方式是把一维数组看成二维数组对待，采用两重循环进行赋值；
	要注意，对于拷贝的方式，类似于网络协议传输中的窗口的概念，通过一个pos变量
	来向后一次移动一下窗口。
	具体的就是首先数据缓冲从显示缓冲里面获取63列数据，第64列数据就从pdata_buf里面获取，
	这样就可以实现连续的显示；pos从0到要显示数据的长度为止。